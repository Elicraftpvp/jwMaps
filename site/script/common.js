// site/script/common.js

// Define o caminho base para todas as chamadas de API.
// Este script deve ser carregado antes de qualquer outro script de página.
const API_BASE_URL = '../backend';
const API_BASE_URL1 = '../../backend';